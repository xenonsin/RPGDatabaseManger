<Window x:Class="RPGDatabaseManager.View.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="800" Width="1200">
    
    <Grid>
        <TabControl TabStripPlacement="Top"  ItemsSource="{Binding Settings}" Background="#FF2D2D30" SelectedIndex="{Binding SelectedTab}">            
            <TabControl.ItemTemplate>
                <DataTemplate>                    
                    <TextBlock Text="{Binding Header}" Margin="20" Foreground="Gray"/>                   
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Frame Content="{Binding Path=ViewPage , Mode=TwoWay}"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
